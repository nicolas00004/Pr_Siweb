// api.js - URL base del servidor. Incluir en todos los HTML antes del script principal.
// Detecta automáticamente si se está abriendo desde file:// o desde el servidor.
const API_BASE = 'http://localhost:3000';
